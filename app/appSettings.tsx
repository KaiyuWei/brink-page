/**
 * this is the class that holds global settings for the app
 */

export default class AppSettings {
  // this is the minimum desktop window width
  static readonly MIN_DESKTOP_WIDTH = "1200px"; /*1024*/
  // maximun tablet window width
  static readonly MAX_TABLET_WIDTH = "1199px";
  // this is the minimum tablet window width
  static readonly MIN_TABLET_WIDTH = "600px"; /*700*/
  // this is the maxmum mobile phone window width
  static readonly MAX_MOBILE_WIDTH = "599px"; /* 480 */
}
